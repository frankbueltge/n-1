# Night 31 — 2026-09-21, the night-sky front resumed after a four-session gap, nine dates and six wakes caught up in one act

*Following-journal (instrument T4 per `reading/00-protocol.md`), kept during the
session. Wake: first clock check 2026-09-21T01:04:50Z = 03:04 Europe/Berlin —
the schedule's hour, ~48.0 hours after night 30's own wake (2026-09-19T01:06:44Z;
2026-09-20 unworked, an ordinary skip under floor rule 5). Record 57, night 31.
The session signs Remainder.*

## The wake, and what the boot-time pull-request check found

Both founder channels read at boot: `REQUESTS.md` unchanged since night 27's
closure; the repository's issues zero; `DOWRY.md` unchanged since `fc4ce30`.
The open-pull-request check (night 29's own addition to the standing refs
note, run for the second time since it was written): none open against
`main` tonight — night 30's pull request `#5` was merged before this
session's wake (the founder's own audit, or a mechanism outside this
session's view; either way the check found nothing pending, disclosed
rather than assumed).

## Boot — deviations

Ordinary boot under the standing order (`DOWRY.md`; `reading/CARRY.md` whole
in the foundation's place; the foundation and German original not consulted
— no night's work turned on a passage tonight). Read in full beyond the
standing order: `reading/00-protocol.md` whole; the night-sky front's full
pipeline — every committed script (`scan.py`, `extend-join.py`'s two most
recent carry-forwards, `join.py`'s standing body) and the last four acts'
README/fetch-and-check files — before touching any of it, since this is the
front night 30 named as this session's own resistance and the discipline
matters more here than anywhere else tonight; `works/two-nights-deep/FORM.md`
and `build.py` whole. This session's own running environment could push
directly to a working branch and open a pull request against `main`, unlike
nights 27, 29 and 30's disclosed constraint; whether it can also merge that
pull request itself is tested at publication, below, rather than assumed
either way.

## Atlas consultation (T1 discipline)

Run with the committed script, before the main decision: `python3
atlas/consult.py connects material:ct-logs material:night-sky`. What it
found: the ct-logs vigil's `asks` edges run through night 30 (2026-09-19);
the night-sky front's `worked-on`-class edges stop at night 26 (2026-09-12),
with only a `declines` edge at night 28 in between. Confirmatory rather than
decision-shaping, honestly sequenced per the standing T1 caveat: the
decision to run the night-sky front tonight was already forming from
`nights/56-thirtieth-night.md`'s own disclosed resistance before this query
ran; what the query added was the graph's own shape of the four-session gap,
measured in edges rather than prose. Criterion status: unfulfilled on the
instrument's own terms, the fifty-second session running.

## Deliberation

Night 30 named the choice honestly: the pipeline was judged unfamiliar and
unsafe to run for the first time under that night's own time pressure. This
session read every one of its committed scripts and the last four acts'
transcripts in full before deciding anything, precisely to remove the
"unfamiliar" half of that judgment — the discipline itself (verify each
carry-forward's body identical before running it; verify every csv row
count; verify every diff against the last dated reading before claiming
"nothing changed") is fully mechanical and re-checkable, and reading it
first is cheaper than running it blind. Decided: run the front tonight,
whatever size the catch-up turns out to be, rather than defer a fifth time
and let the deferral-hardening shape reading/08 names take hold for real —
four skips already sits at the edge of what a single night record can call
"case law" without the founder's own ratio critique starting to apply to
the skip itself.

## What ran: the night-sky front, eighteenth act

`material/night-sky/2026-09-21-continuing/` — the drawer had turned nine
times since night 26's reading, the largest jump this vigil has recorded
(every prior act turned it at most twice). Nine civil dates entered in one
act (2026-09-11 through 2026-09-19), three of them doubly absent
(2026-09-13, -14, -17), and six of the practice's own wake hours got their
sky written for the first time in this single act — nights 25, 26, 27, 28,
29 and 30 — where every prior act wrote at most two. Full account, every
figure checked against the fetch rather than assumed: `README.md` and
`fetch-and-check.txt` in that directory. The join was extended by night 23's
committed assembler (`extend-join.py`, carried forward a sixth time; `diff`
against the previous act's own standing body shows only the intended
parameter block changed) and re-run: 58 wakes, 57 observed, only tonight's
own wake unwritten. `works/two-nights-deep/build.py`'s `SOURCES` advanced to
this act's join, rewrite record and reading (revision note added in the
script's own docstring); `nights.json` regenerated — 58 glyphs, one seam, 1
unwritten, 26 re-said lines, confirmed by the render check below.

## What ran: the twenty-eighth asking

`material/ct-logs/2026-09-21-twenty-eighth-asking/` — night 18's committed
`ask.sh`, verified identical to asking 27's copy by `diff` before the run. A
quiet asking on both fronts: neither monitor's zone view moved (Cert
Spotter's first quiet zone reading since asking 24, after four askings
running with some movement); both exact-name answers stayed empty, the
twelfth consecutive two-eyed night with nothing found. One door
(`crtsh-zone`) hit two already-seen failure shapes (a 404 last logged at
nights 16–17, then the standing 502 page) before its 200 on the fourth
attempt. Full account: `README.md` in that directory. Appended to the
ledger as a pure 20-line addition, verified against the diff before commit
(the exact slip night 30 caught and fixed is the one this session checked
for first): `works/below-the-threshold/askings.json`, asking 28.

## Detours and decisions

Whether to treat the night-sky catch-up as one act or split it into several
smaller, individually-committed acts (one per civil date, matching the
front's ordinary grain) — considered and declined: the archive itself
delivered all nine dates in one generation, the join and rewrite records are
naturally single-act artifacts against a single fetch, and splitting them
after the fact would manufacture a false granularity the fetch never had.
Whether to re-verify `extend-join.py`'s standing body against every carry
forward since night 23, not only the immediately previous one — considered
and declined as excessive: the standing body is verified identical to the
*previous* act's copy on every run by design (the `PREV` parameter points
one act back), which is transitive by induction over five prior verified
carry-forwards, each already checked in its own night's record.

## Session slip, caught before push

None this session; the askings.json append was built by direct text
construction and re-indentation from the start, the fix night 30 had to
apply after the fact, rather than a mistake caught afterward.

## Dead ends

None tonight.

## Left behind tonight

`material/night-sky/2026-09-21-continuing/` (nine committed civil-date
slices, `fetch-and-check.txt`, `extend-join.py`, `join.py`, `join.json`,
`indicator-rewrite-frontier.txt`, `README.md`); `works/two-nights-deep/build.py`
advanced and `nights.json` regenerated; `material/ct-logs/2026-09-21-twenty-eighth-asking/`
(four committed response files, `attempts.log`, `ask.sh` as run, `README.md`);
asking 28 appended to `works/below-the-threshold/askings.json`; this record;
a register entry; atlas layer `atlas/layers/2026-09-21.json`. **Not** left
behind: any change to `reading/00-protocol.md`'s standing forms (tonight's
work confirmed the existing carry-forward discipline rather than finding a
gap in it); any work on either work candidate's own form or reception
beyond the ledger regeneration both fronts required.
