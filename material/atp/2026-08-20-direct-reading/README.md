# ATP direct reading — 2026-08-20 (night 07, record 24)

The verification run for `reading/17-consolidation-the-hole.md`, the reading's second
entry written from the edition directly. Source: the founder's private
`frankbueltge/material` (`atp.pages.txt`, the page-tagged extraction of Deleuze/
Guattari, *A Thousand Plateaus*, trans. Massumi, Minneapolis 1987; repository HEAD
489de9d, unchanged since bell 17 — the reach re-verified at this session's boot). No
text of the edition is committed here: verdicts, page numbers, marker status and
fragment lengths only (floor rule 2 as amended). Method: letter-only normalization
per the material README's caveat; `verify.py` is re-runnable by any reader with
access to the private material.

## Result

**22 of 22 quotations stand on their cited pages.** 18 on confirmed printed markers
(pages 328, 329, 344); 4 on markers the extraction could not confirm, with the
printed number inferred from confirmed neighbours (313 between confirmed 312/314;
345 between confirmed 344/346) — the same "inferred" class the re-verification of
2026-08-18 established and disclosed. Full verdicts: `results.json`.

## A collation datum, of entry 15's class

KsK v3 (T5, misuse paragraph; both language versions) quotes the refrain plateau's
warning as "a scribble effacing all sounds", cited ATP 343–344. In this extraction:

- the phrase as KsK quotes it appears **nowhere** (letters-only search, whole file);
- page 344 carries the doubled phrase "a scribble effacing all lines, a scramble
  effacing all sounds";
- page 343 carries no part of it (no "scribble", "scramble" or "effacing").

Read at its exact size: KsK's quotation conflates two parallel phrases into one —
the same faithful-propagation class as the ATP 82 datum of
`reading/15-verification-the-copy-chain.md` — and the finding is offered to the
foundation's own editorial reservation (collation against the print edition before
any print publication). The OCR caveat is stated: the extraction, not the printed
page, is what was searched; the doubled phrase is syntactically clean and parallel,
which an OCR error would be unlikely to produce, but the print collation is the
authority. Nothing in KsK's argument turns on the difference — the misuse warning
(T5's "cosmic dilettantism") survives either wording.

## Files

- `quotes.json` — the 22 quotations exactly as entry 17 carries them, with pages.
- `verify.py` — the method, re-runnable.
- `results.json` — verdicts, marker statuses, the three collation-datum checks.
