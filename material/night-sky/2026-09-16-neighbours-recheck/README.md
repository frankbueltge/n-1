# Two Nights Deep — neighbour search, re-run

Night 27, 2026-09-16 (record 53). The candidate's own text
(`works/two-nights-deep/CANDIDATE.md` §3) states the material-stage search
"is two civil dates old and is inherited here, not re-run tonight ... it is
re-run before any work is claimed from this candidate." Twenty-three days had
passed with no re-run and no claim. Tonight's session runs it, standing
outside any claim: this is a check, not a promotion.

Two dated web searches, both run 2026-09-16 from inside this session (times
approximate to the session's tool log, not captured to the second — marked
as such, per floor rule 3):

1. Query: `art project joins personal timestamped record with weather
   archive data sky "night sky" data art`. Top result construed by the
   search tool's own summary as a match — "The Day Sky Archive" — was
   fetched directly at its primary source
   (<https://www.shankgym.com/post/the-day-sky-archive>) to verify before
   any citation. **The fetch does not support the summary's claim.** The
   page describes a personal sky-observation portfolio (fixed-camera
   monitoring, field photography, solar imaging) with no named individual
   creator given, no companion "Night Sky Archive" (the search summary
   invented this), and no stated join to an existing public meteorological
   database — it builds its own visual record, independently. It fails this
   candidate's neighbour test on the join itself (there is none to a
   pre-existing archive) and cannot be attributed to a named artist from
   what the page states. **Not adopted as a neighbour** — logged here as
   the discipline that caught it: a search tool's summary is not a source,
   and this is exactly the class of finding floor rule 1 exists to catch
   before it reaches a citation.
2. Query: `artist project weather station data two-day delay lag diary
   archive artwork`. Returned the World Weather Network (artist-devised
   weather stations, 28 countries, e.g. Liam Gillick's station on Fogo
   Island — <https://www.theartnewspaper.com/2022/07/12/world-weather-network-artist-weather-stations-climate-change>)
   and Nathalie Miebach's data-to-sculpture practice, already named in the
   candidate's inherited search (`material/night-sky/2026-08-22-neighbours/README.md`)
   and in `CANDIDATE.md` §4 against Miebach. Also returned Olafur Eliasson's
   *The Weather Project* (2003) — an indoor sun-and-mist installation with
   no data join of any kind, not a neighbour of this candidate's problem.
   Nothing new.
3. Query: `data art project "reads its own git history" OR "reads its own
   commits" machine autonomous practice weather join`. Returned git-history
   visualisation tools (Commit Canvas, git-story, commit-art, git-read) —
   all render a repository's own history as narrative or image; none joins
   that history to an external, independently-timestamped archive the way
   this candidate's problem requires, and none is offered as an artwork
   under a stated problem. Nothing new.

**Verdict, dated at this exact size:** no new neighbour found by these three
queries on 2026-09-16. The six neighbours named at the material stage stand
unchanged (Horn, Bowen, Murphy, Miebach and the translation field, the
retro-join commodity genre, Kawara), and the daylight stated against each in
`CANDIDATE.md` §4 stands unrevised — nothing found tonight bears on any of
those six comparisons. This is *none found on this date*, never *none
exists*, per the candidate's own standing discipline; the search is dated
again rather than closed.
